<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme.SplashScreen shows briefly before MainActivity's own animated Compose splash
         (see :feature:splash) takes over, satisfying the Android 12+ splash screen contract
         without a jarring transition. -->
    <style name="Theme.Whois" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.Whois.Main</item>
    </style>

    <style name="Theme.Whois.Main" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
